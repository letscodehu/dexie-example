{
    "dependencies": {
        "jest": "^29.5.0"
    },
    "scripts": {
        "test": "NODE_OPTIONS=--experimental-vm-modules npx jest",
        "build": "npx tsc",
        "clean" : "rm -rf node_modules/ dist/"
    },
    "main" : "dist",
    "files": [
        "dist/*"
    ],
    "devDependencies": {
        "@types/jest": "^29.5.1",
        "ts-jest": "^29.1.0",
        "ts-node": "^10.9.1",
        "tsc": "^2.0.4",
        "typescript": "^5.0.4"
    }
}
